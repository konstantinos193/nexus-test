version: '3.8'

services:
  admin:
    image: ghcr.io/yourusername/martech-admin:production
    environment:
      NEXT_PUBLIC_API_URL: https://yourdomain.com/api
      NEXT_PUBLIC_APP_URL: https://yourdomain.com/admin
    networks:
      - martech-production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  martech-production:
    external: true
